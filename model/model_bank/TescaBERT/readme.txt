The models are saved here.
If you run the file run.sh, the model will be saved in /try.
If you run the file loss.sh, the model will be saved in /loss_rate_{$loss_ratio}.
If you run the file noise.sh, the model will be saved in /noise_{$noise_rate}.